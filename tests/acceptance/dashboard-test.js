import { test } from 'qunit'; // eslint-disable-line
import moduleForAcceptance from 'taxinator/tests/helpers/module-for-acceptance'; // eslint-disable-line
moduleForAcceptance('Acceptance | dashboard');

test('should redirect to dashboard route', (assert) => {
  assert.ok(true);
});
